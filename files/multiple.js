const occupation = "Software Engineer";
const age = 30;

if (occupation === "Software Engineer" && age > 25) {
    console.log("The person is a Software Engineer and older than 25.");
} else {
    console.log("The person does not meet both criteria.");
}